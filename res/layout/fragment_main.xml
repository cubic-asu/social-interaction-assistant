<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="edu.asu.cubic.app.MainActivity$PlaceholderFragment" >
    
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:padding="24dp"
        android:textAppearance="?android:attr/textAppearanceLarge"
        android:text="@string/select_option"
        android:textSize="20sp"
        android:contentDescription="@string/select_description"
        android:focusable="true" />

    <RadioGroup
        android:id="@+id/rg"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_weight="6" >

        <RadioButton
            android:id="@+id/rbtnEmotions"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="@string/emotions"
            android:textSize="18sp"
            android:contentDescription="@string/emotions"
            android:focusable="true" 
            android:onClick="onRbtnClicked"/>

        <RadioButton
            android:id="@+id/rbtnActionUnits"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="@string/action_units"
            android:textSize="18sp"
            android:contentDescription="@string/action_units" 
            android:focusable="true" 
            android:onClick="onRbtnClicked"/>

    </RadioGroup>

    <Button
        android:id="@+id/btnSelect"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:text="@string/select"
        android:textSize="18sp"
        android:contentDescription="@string/select"
        android:focusable="true"
        android:onClick="initiateApplication" />

</LinearLayout>